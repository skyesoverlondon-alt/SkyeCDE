 first i need to have the file that is in the background of our UI that is appearing silver and gray to become a money green and we should have the background seperated from the actual UI.
 
 The Theia shit is just an extension so my 0s is easier to use. The 0s existed prior to theia in terms of my creation process, so Theia should not be treated as the main shit at repo root even if that is what it is being used at build time, does this make sense???? my repo needs to reflect the actual truth which is the theia buildout was just an upgrade to my platform not the beginning point. 
 
  and also some parts of my 0s like the actual code editors were not all the way built out functionally meaning they had some minor bugs but the plumbing was pretty good in terms of the push capabilities and all of that and that should also be touched on somewhat in the kaixu multi modal patches we used to upgrade the 0mega gate. case and point the skydex. that could be used to launch right inside of the CDE but the Skydex should be upgraded so that it uses whatever features theia has that makes theia actually functional does this make sense???? The end result is SkyDex should actually be autonomous and have a version of the CDE that opens and runs that 



  I also want my entire app and platform registry to be listed under the built in extensions in a way where a user can just open the extensions tab, click the drop down for built-in and see icons for every app in our registry 

  we need to build out an implementation directive with file by file edits so there is no room for error and have that start at line 13 of this document. DO NOT OVERWRITEA ANYTHING IN THIS FILE YOU START AT LINE 13


## IMPLEMENTATION DIRECTIVE: 0s launcher improvements + gate normalization

### 1. Non-negotiable implementation rule

- Every AI-facing launch surface inside the 0s layer must normalize to 0megaSkyeGate first.
- The canonical gate implementation for this repo is /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/0megaSkyeGate/0megaSkyeGate-The-Actual-Gate.
- Any `OMEGA_GATE_URL` value in app code or env is the deployed runtime endpoint for that internal gate, not a different gate source of truth.
- Pre-deploy failures are expected until that internal gate is actually deployed and its env vars are installed; wiring should still be completed now so deployment is just env population and cutover.
- No app in the launcher flow should call OpenAI, Anthropic, Gemini, or any other provider directly from product code if the same request can be routed through the gate.
- Theia remains the shell upgrade, not the origin story. The launcher, app registry, and built-in extension surfaces must present the 0s as primary and Theia as the support layer.

### 2. Gate normalization scope

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyDex/SkyDex4_fixed/netlify/functions/ai-agent.js

Required edits:
- Remove the direct `OPENAI_API_KEY` hard dependency and the direct `https://api.openai.com/v1/responses` call.
- Replace provider execution with a gate client path that targets the internal 0megaSkyeGate implementation, using `OMEGA_GATE_URL` only as that gate's runtime endpoint.
- Add env support for `OMEGA_GATE_URL`, `KAIXU_APP_TOKEN`, and a default gate alias such as `kaixu/code`.
- Convert the request payload so the SkyDex agent sends its structured agent prompt through the gate instead of directly to OpenAI.
- Preserve the current JSON-schema response contract so the front-end does not break while routing is changed.
- Keep the bounded workspace behavior, but make the gate the execution lane.

Implementation note:
- SkyDex is currently the clearest proof that some calls are still not normalized. This file is the first required fix.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyDex/SkyDex4_fixed/index.html

Required edits:
- Replace all user-facing copy that says SkyDex uses direct OpenAI secret agenting.
- Rename the agent control messaging so it refers to Kaixu or 0mega gate lanes, not direct OpenAI.
- Update the runtime AI touchpoint detection to treat gate paths as the expected normalized pattern.
- Update the error message for `/api/ai-agent` so it points to missing gate configuration instead of missing `OPENAI_API_KEY`.

Implementation note:
- The UI copy must stop reinforcing the old architecture after the backend is moved.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyDex/SkyDex4_fixed/README.md

Required edits:
- Rewrite the architecture note so SkyDex is described as a gate-routed app.
- Remove the statement that OpenAI still runs directly server-side.
- Document the new gate env contract and the alias expected by SkyDex.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyeMail2.0/assets/kaixuGateway.js

Required edits:
- Treat this file as the canonical browser-side pattern for normalized gate calls.
- Extract any reusable parts from here into a shared gate client module if multiple apps need the same chat and stream helpers.
- Standardize all launchable apps on the same base URL resolution order:
    1. `window.OMEGA_GATE_URL`
    2. local persisted gate URL
    3. platform default gate URL

Implementation note:
- This file is already close to the desired end state and should be used as the reference implementation.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/Kaixu67/_shared/config.js

Required edits:
- Expand this shared config into the common gate bootstrap helper for launcher-launched apps.
- Add helpers for:
    - gate URL resolution
    - app token/key hydration
    - persistent workbench import
    - normalized status messaging
- Use this file as the shared client config layer for any app surfaced from the launcher.

Implementation note:
- This is the right place to centralize browser-side gate normalization so each app stops inventing its own wiring.

### 3. Launcher and shell truth

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/skye-studio-launcher-widget.tsx

Required edits:
- Replace the current generic launcher sections with 0s-native app launch sections.
- Add explicit launch cards for the real platform apps in your registry instead of treating extensions as the main discovery surface.
- Add a dedicated action for opening SkyDex in autonomous mode as a first-class launch path.
- Keep the Explorer/Settings/Theia utility actions, but visually subordinate them under the 0s launcher actions.
- Replace the current OpenVSX-heavy wording with platform-registry wording.

Implementation note:
- This widget is where the shell currently tells the wrong story about what the product is.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/skyes-over-london-getting-started-widget.tsx

Required edits:
- Reframe the welcome surface so the 0s platform apps are the main entry point and Theia workflow panels are secondary.
- Add a section sourced from the app registry that lists launchable platform apps.
- Add a section that explains that gate-routed apps are the default execution model.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/skyes-over-london-about-dialog.tsx

Required edits:
- Add a platform registry section alongside installed extensions.
- Show built-in 0s apps separately from marketplace extensions.
- Treat extensions as shell add-ons, not the identity of the product.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/branding-util.tsx

Required edits:
- Remove or downgrade copy that frames OpenVSX as the main extension story.
- Add helper renderers for built-in platform apps and registry-driven app categories.
- Add branded descriptions that explain the shell truth correctly: the 0s came first, Theia is the functional shell upgrade.

### 4. Built-in extension registry requirement

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/skyes-over-london-contribution.tsx

Required edits:
- Add commands and menus for opening a built-in platform registry view.
- Register an explicit command that opens the 0s app registry from the extensions area.
- Wire a command that can launch a selected registry app or open its panel.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/skyes-over-london-frontend-module.ts

Required edits:
- Bind a new registry contribution or widget that feeds the built-in extensions area with platform app entries.
- Register any necessary widget factory, contribution, or service for the built-in app list.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/package.json

Required edits:
- Add any additional dependency needed for the built-in app registry widget if the existing product extension package does not already expose the required UI pieces.
- Do not add generic marketplace logic here; only add what is needed for the platform registry surface.

Implementation note:
- The desired outcome is that when a user opens Extensions and chooses Built-in, they can see your registry apps with icons and descriptions, not just default Theia/OpenVSX items.

### 5. Background separation and money-green visual pass

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/style/skye-supra-eye-v2.css

Required edits:
- Change the background treatment from the current silver-gray feel to a money-green atmospheric layer.
- Separate the ambient background plane from the actual panel/UI surfaces so the UI reads as a foreground object instead of blending into the shell.
- Keep dark contrast, but shift the accent system away from looking metallic gray.
- Define separate variables for:
    - ambient background wash
    - panel surface
    - panel elevated surface
    - borders
    - launcher accent
    - gate-active green
- Ensure the launcher hero, dashboard cards, and workbench chrome inherit from the separated layer model.

Implementation note:
- The actual request here is not just recolor; it is visual layer separation.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/product/src/browser/style/index.css

Required edits:
- Add any shell-level class hooks required for the new ambient-background-versus-UI split.
- Keep the layout stable while the theme variables are swapped.

### 6. Launcher service and app-launch normalization

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/launcher/src/browser/launcher-service.ts

Required edits:
- Extend launcher metadata so a launch target can declare whether it requires gate bootstrap on open.
- Support passing normalized gate URL and session token material into launcher-opened experiences where appropriate.
- Add a clean model for launching first-party 0s apps, not just shell utilities.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/theia-extensions/launcher/src/browser/create-launcher-contribution.ts

Required edits:
- Update the launcher initialization flow so it reflects 0s launch behavior instead of a generic studio launcher install.
- If needed, rename prompts and messaging to reference 0s launcher language instead of Theia launcher language.

### 7. Registry source of truth

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/public/S0L26-0s/hub-registry.json

Required edits:
- Treat this as one of the candidate sources of truth for the app registry.
- Add the icon, label, description, launcher action, and built-in extension grouping fields needed by the product extension UI.
- Make sure every registry app can declare whether it is:
    - built-in launcher app
    - built-in extension tile
    - gate-routed AI surface
    - external or internal panel target

Implementation note:
- If another registry file is more canonical than this one, standardize on one and stop duplicating launcher metadata.

### 8. SkyDex functional upgrade inside the CDE

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyDex/SkyDex4_fixed/index.html

Additional required edits:
- Add a mode that lets SkyDex open as a CDE-native workspace panel instead of feeling like a separate external app.
- Reuse Theia capabilities where helpful:
    - file tree awareness
    - current workspace context
    - change review surfaces
    - command palette integration
    - extension-host-backed editor behavior where possible
- Keep SkyDex sovereign as an app, but let it borrow Theia functionality instead of re-solving solved shell features badly.

#### File: /workspaces/SkyeCDE/Skye0s-s0l26/Sky0s-Platforms/SkyDex/SkyDex4_fixed/TEST_REPORT.md

Required edits:
- Rewrite test expectations around gate routing once the agent lane is normalized.
- Remove any claim that live execution depends on direct OpenAI env configuration.

### 9. Implementation order

1. [✓] Normalize SkyDex backend and front-end references to the gate.
2. [✓] Extract or standardize the shared gate bootstrap module from the existing gate-aware client code.
3. [✓] Rework the product launcher widget so the 0s apps are primary and Theia actions are secondary.
4. [✓] Add the built-in registry view and wire it into the extensions/built-in surfaces.
5. [✓] Apply the money-green background separation pass to the shell theme.
6. [✓] Finish SkyDex-in-CDE integration so it launches as a real autonomous 0s surface inside the shell.

### 10. Definition of done

- SkyDex no longer calls OpenAI directly.
- Launcher-discovered AI apps use normalized gate bootstrap logic.
- The product UI tells the truth that the 0s is primary and Theia is the upgrade layer.
- The built-in extensions surface lists the platform registry apps with icons.
- The background no longer reads silver/gray and is visually separated from the UI layer.
- SkyDex launches inside the CDE as a proper autonomous platform surface using shell features where they help.